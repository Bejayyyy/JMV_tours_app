<!-- res/drawable/rounded_textfield.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Background color -->
    <corners android:radius="15dp" /> <!-- Corner radius -->
    <stroke
        android:width="1dp"
        android:color="@color/blue" /> <!-- Border color -->
</shape>
